{
  "name": "carcel.dev",
  "private": true,
  "scripts": {
    "serve": "http-server -o",
    "stylelint": "stylelint 'public/**/*.{css,less,html}'"
  },
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.12.3",
    "stylelint": "^13.7.2",
    "stylelint-config-standard": "^20.0.0"
  }
}
